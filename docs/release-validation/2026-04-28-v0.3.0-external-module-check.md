# v0.3.0 External Module Check

- Date: 2026-04-28
- Repository: /Users/liu_y/code/goProject/simplykb
- Status: passed
- Purpose: verify that the published `v0.3.0` tag is fetchable from a fresh external Go module and compiles against the documented public surface.

## Command

Run from a temporary directory outside this repository:

```bash
go mod init example.com/simplykb-check
go get github.com/LyleLiu666/simplykb@v0.3.0
go mod tidy
go test ./...
```

The temporary module also compiled a `main.go` that references:

- `New`
- `Config`
- `Close`, `Ping`, `Migrate`, and `EnsureSchema`
- `UpsertDocument`, `ReindexDocument`, and `DeleteDocument`
- `Search` and `SearchDetailed`
- `NewDefaultSplitter`
- `NewHashEmbedder`
- `SearchModeVector`
- `QueryEmbeddingCacheStatusDisabled`
- `DocumentStats`, `SearchHit`, `SearchResponse`, `SearchDiagnostics`, and `ChunkDraft`

## Result

```text
go: added github.com/LyleLiu666/simplykb v0.3.0
?    example.com/simplykb-check    [no test files]
```

This supersedes the tag-fetchability concern recorded in [2026-04-24-v1.0.0-blocker-evidence.md](./2026-04-24-v1.0.0-blocker-evidence.md) for `v0.3.0`.
It does not approve any future `v1.0.0` tag; future release candidates still need their own external module check.
